name: Publicar OfficeJur

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Baixar repositório
        uses: actions/checkout@v6

      - name: Configurar Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: apps/validador-projudi/package-lock.json

      - name: Montar Validador Projudi
        working-directory: apps/validador-projudi
        run: npm ci && npm run check

      - name: Testar regras financeiras
        working-directory: apps/financeiro
        run: npm test

      - name: Testar integração com Mercado Pago
        working-directory: apps/financeiro/worker
        run: npm test

      - name: Configurar GitHub Pages
        if: github.event_name != 'pull_request'
        uses: actions/configure-pages@v6

      - name: Montar site estático
        run: ./scripts/build-site.sh

      - name: Validar site publicado
        run: node ./scripts/validate-site.mjs

      - name: Confirmar bundles rastreados
        run: git diff --exit-code -- apps/validador-projudi/assets

      - name: Enviar artefato do site
        if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@v5
        with:
          path: _site

  deploy:
    if: github.event_name != 'pull_request'
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Publicar no GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
